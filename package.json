{
  "name": "validori",
  "version": "0.1.0",
  "description": "Idk what this is, but its a validation thing so.",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf coverage; rm -rf .nyc_output; rm -rf dist",
    "ts-node": "TS_NODE_PROJECT=tsconfig.json ts-node-script ./exp.ts",
    "build": "npm run clean && tsc --project tsconfig.json",
    "prepare": "npm run build"
  },
  "dependencies": {
    "olive-data-contracts": "git+ssh://git@bitbucket.org/crosschx/olive-data-contracts.git#master",
    "ts-node": "^9.1.1",
    "typescript": "^4.2.2"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
